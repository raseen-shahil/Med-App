import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

export default function WishlistScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#FFFFFF' }}>
      <ThemedText>Wishlist Screen</ThemedText>
    </View>
  );
}